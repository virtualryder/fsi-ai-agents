# Payments Compliance Agent — Python Dependencies
# Pin exact versions for reproducible regulatory-grade deployments.
# After any update, re-run tests and re-validate compliance properties
# before deploying to production.

# ── LangGraph / LangChain ─────────────────────────────────────────────────────
langgraph==0.2.28
langchain==0.3.7
langchain-openai==0.2.8
langchain-core==0.3.15

# ── OpenAI ────────────────────────────────────────────────────────────────────
openai==1.54.3

# ── Streamlit Dashboard ───────────────────────────────────────────────────────
streamlit==1.40.1

# ── Date / Time (SLA deadline calculations) ──────────────────────────────────
python-dateutil==2.9.0.post0

# ── Environment / Configuration ───────────────────────────────────────────────
python-dotenv==1.0.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0

# ── Optional: Production checkpoint store ─────────────────────────────────────
# Uncomment for production deployment with PostgreSQL persistence.
# Required for durable HITL review queue that survives process restarts.
# langgraph-checkpoint-postgres==2.0.3
# psycopg2-binary==2.9.10

# ── Optional: AWS Secrets Manager (production only) ──────────────────────────
# boto3==1.35.54
