# Document Intelligence Agent — Python Dependencies
# ============================================================
# These requirements are pinned for reproducibility and security.
# When upgrading, test all 12 graph nodes and run the full test
# suite before deploying to production.
#
# Production note: In ECS, dependencies are baked into the Docker
# image at build time. The image is scanned by Amazon ECR Enhanced
# Scanning (Trivy) for known CVEs before deployment.
# ============================================================

# ── LangGraph / LangChain (core agent framework) ─────────────
langgraph>=0.2.0
langchain>=0.2.0
langchain-anthropic>=0.2.0

# ── LangGraph PostgreSQL checkpointer (production only) ──────
# Install this only in production ECS environments.
# Comment out for local development to avoid requiring PostgreSQL.
# langgraph-checkpoint-postgres>=0.1.0

# ── OpenAI client ─────────────────────────────────────────────
anthropic>=0.40.0

# ── Document Parsing ──────────────────────────────────────────
# PDF extraction (native text layer — no OCR required for digital PDFs)
pdfplumber>=0.11.0
PyMuPDF>=1.24.0          # fitz — faster PDF text extraction, image extraction

# OCR for scanned documents (images, scanned PDFs)
pytesseract>=0.3.13      # Tesseract OCR Python wrapper
Pillow>=10.0.0           # Image processing (required by pytesseract)

# Word document parsing
python-docx>=1.1.0

# ── Streamlit UI ──────────────────────────────────────────────
streamlit>=1.36.0

# ── Environment / Security ────────────────────────────────────
python-dotenv>=1.0.0

# ── Utilities ─────────────────────────────────────────────────
typing_extensions>=4.10.0
pydantic>=2.7.0
httpx>=0.27.0            # HTTP client (used by openai library)

# ── Testing ───────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
