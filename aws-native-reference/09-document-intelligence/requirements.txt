# AWS-native Document Intelligence reference.
# The deterministic core + pipeline run with only the stdlib (demo mode).
# Live inference adds the Strands SDK (which uses boto3 for Bedrock).
strands-agents>=1.0.0   # LLM drafting via Amazon Bedrock (optional for demo)
pydantic>=2.7           # structured extraction schema
boto3>=1.34             # Bedrock + Step Functions + DynamoDB (live only)
# Test-only:
pytest>=8.0
