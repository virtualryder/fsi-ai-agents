version: "3.9"

services:
  fraud-detection-agent:
    build: .
    ports:
      - "8504:8504"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
