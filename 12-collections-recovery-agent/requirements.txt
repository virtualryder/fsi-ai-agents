# Agent 12 — Collections & Recovery Agent
# Compatible ranges — the previous exact pins were mutually unresolvable
# (pip ResolutionImpossible on langgraph-checkpoint==2.0.2 vs langgraph==0.2.28).
# For true reproducibility, generate a lockfile: pip-compile or `uv pip compile`.

# LangGraph + LangChain core
langgraph>=1.0,<2.0
langchain>=1.0,<2.0
langchain-anthropic>=0.2.0
langchain-core>=1.0,<2.0

# LangGraph checkpoint persistence (production: PostgresSaver)
langgraph-checkpoint>=4.0,<5.0
langgraph-checkpoint-postgres>=3.0

# Database
psycopg2-binary==2.9.10     # Aurora PostgreSQL (production)
boto3==1.35.36               # S3 Object Lock, DynamoDB, Secrets Manager, CloudWatch

# Data processing
pandas==2.2.3                # Debt portfolio analytics, SOL matrix lookups
numpy==1.26.4                # Payment plan optimization math

# UI
streamlit>=1.40.1

# Utilities
python-dateutil==2.9.0.post0  # FDCPA time-of-day + business day calculations
pytz==2024.1                   # Consumer local timezone enforcement (FDCPA 8am-9pm)
pydantic==2.9.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
